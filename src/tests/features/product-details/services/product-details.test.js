import { describe, it, expect } from 'vitest';
import { getProductsDetails } from '../../../../features/product-details/services/product-details';

describe('', () => {
    it('It should return the products details', () => {
        
        expect(1).toStrictEqual(1);
    });
});